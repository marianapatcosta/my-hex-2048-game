import * as utils /* { utils } */ from './utils'

describe('useHexagonDimensions', () => {

  test('should return `http://localhost:8080/4` as url and 4 as radius value, if radius is 2', () => {
    
  })
})
